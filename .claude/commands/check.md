---
description: Format with black and run the test suite
---

Run `black app` to format the code, then `python -m pytest -q` to run the tests.
Report the formatting result and the test summary. If any test fails, show the
failing test output and stop.
